@model PlanUp.Models.Song[]

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    @if (Model != null)
    {
        foreach (var item in Model)
        {
            <tr>
                <td>
                    <iframe width="420" height="315"
                            src="@item.Etag">
                    </iframe>
                </td>
            </tr>
        }
    }
</table>
